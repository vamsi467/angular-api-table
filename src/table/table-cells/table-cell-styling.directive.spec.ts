import { TableCellStylingDirective } from './table-cell-styling.directive';

describe('TableCellStylingDirective', () => {
  it('should create an instance', () => {
    const directive = new TableCellStylingDirective(null);
    expect(directive).toBeTruthy();
  });
});
